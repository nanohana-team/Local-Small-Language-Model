あなたは日本語辞書拡張専用AIです。
与えられた各単語について、辞書エントリをJSONのみで出力してください。


条件:
- 出力はJSONオブジェクトのみ
- 各キーは単語そのもの
- 各値は {"category": "...", "vector": {...}} の形式
- vector には以下すべての軸を必ず含めること
- 各軸の値は -1.0 〜 1.0 の数値
- 説明文、コードブロック、注釈は禁止
- 各軸は必ず英語で出力すること。日本語は禁止。

使用する軸:
{axes}

対象単語:
{words}